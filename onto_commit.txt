fix: resolve revive stutter warnings and update CI for Go 1.26

Remove package-name prefixes from three exported types that the revive
'sayRepetitiveInsteadOfStutters' rule flagged — the package already
provides the disambiguating prefix at every call site.

Renames (no behaviour changes):
- universe.UniverseAggregate / NewUniverseAggregate
  → universe.Aggregate / NewAggregate
- universe.UniverseRepository
  → universe.Repository
- exploration.ExplorationEntity / NewExplorationEntity
  → exploration.Entity / NewEntity

All downstream files updated: domain services (quantum.go, timeline.go,
generator.go), domain navigation interface, all application commands and
queries, infrastructure implementations, interface/cli, mocks, and tests.

Mockery config (.mockery.yaml) updated to match the current interface
names (LocationGeneratorService, PathfinderService). Stale generated
mocks (mock_Pathfinder.go, mock_LocationGenerator.go) removed and
replaced with correctly named files.

CI fix: bump golangci-lint-action from v6 to v7 and pin golangci-lint
to v2.12.2. The v6 action installs v1.x binaries built with Go 1.24,
which cannot parse a module declaring go 1.26.5. Go 1.26 support landed
in golangci-lint v2.9.0 (February 2026); v7 of the action is required
to use v2 releases.

golangci-lint run ./... now reports 0 issues. All tests pass.
