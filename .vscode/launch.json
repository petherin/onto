{
  // Run `make debug` (or `make debug-dashboard`) in a terminal first, then
  // start this "Attach" configuration to connect VS Code's debugger to it.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Onto (Delve)",
      "type": "go",
      "request": "attach",
      "mode": "remote",
      "port": 2345,
      "host": "127.0.0.1"
    }
  ]
}
